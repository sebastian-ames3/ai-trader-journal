# Supabase PostgreSQL connection string with pgbouncer for connection pooling
# Format: postgresql://postgres.[project-ref]:[password]@db.[project-ref].supabase.co:6543/postgres?pgbouncer=true
DATABASE_URL="postgresql://postgres.xxx:your_password@db.xxx.supabase.co:6543/postgres?pgbouncer=true"

# Supabase password (used by PostgreSQL MCP server)
# Same password as in DATABASE_URL
SUPABASE_PASSWORD="your_password"

# Anthropic API Key - PRIMARY AI PROVIDER
# Powers: text analysis, vision/OCR, chart analysis, AI coaching, pattern detection
# Models: claude-haiku-4-latest (fast), claude-sonnet-4-latest (balanced), claude-opus-4-latest (deep)
# Get your API key from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY="sk-ant-..."

# OpenAI API Key - OPTIONAL (only needed if using Whisper for transcription)
# Only required if TRANSCRIPTION_PROVIDER="whisper" and no AssemblyAI key
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=""

# AssemblyAI API Key for audio transcription (alternative to Whisper)
# Better proper noun recognition (stock tickers), 30% fewer hallucinations
# Get your API key from: https://www.assemblyai.com/app/account
ASSEMBLYAI_API_KEY=""

# Transcription provider: "whisper" (default) or "assemblyai"
# AssemblyAI recommended for trading journals due to better ticker recognition
TRANSCRIPTION_PROVIDER="whisper"

# Options Data Service URL (Python FastAPI microservice)
# Local development: http://localhost:8000
# Production: https://your-service.railway.app
OPTIONS_SERVICE_URL="http://localhost:8000"

# Debug logging (set to 1 to enable detailed logging)
DEBUG=0